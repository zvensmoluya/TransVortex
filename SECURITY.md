# TransVortex 安全策略

## 支持范围

TransVortex 目前处于早期发布阶段。首个公开版本发布后，项目只为最新公开版本提供安全修复；开发分支、历史提交、已被新版本替代的候选安装包和第三方转存包不单独维护。可验证的公开版本与安装包以 [GitHub Releases](https://github.com/zvensmoluya/TransVortex/releases) 为准。

`0.1.0` 的 Windows 安装包没有 Authenticode 签名，因此 Windows 可能显示“未知发布者”或 SmartScreen 提示。这是已披露的发布限制，不应被视为安装包来源可信的依据。安装前仍需核对 Release 页面公布的文件名和 SHA-256。

## 报告安全问题

以下问题可能需要私密报告：

- API key、token、凭据文件或用户数据可能被未授权读取或写入。
- 处理媒体、字幕、配置、任务工件或模型文件时可能造成任意代码执行、命令注入或目录越界。
- 安装、升级、卸载、资源下载或校验流程可能破坏用户数据，或者接受被篡改的发布资产。
- 日志、诊断输出、通知或错误信息可能泄露未脱敏的敏感内容。

不要为这类问题创建包含细节的公开 Issue。请发送邮件至 [open@zven.cc](mailto:open@zven.cc)，主题以 `[Security]` 开头；这是 TransVortex 唯一的公开联系邮箱。

报告中请尽量提供受影响版本、影响范围、必要前置条件、最小复现步骤和已经验证的缓解方式。所有日志、路径和配置都应先脱敏；不要发送真实 API key、`auth.json`、私有媒体或完整任务目录。

## 普通缺陷与第三方服务

不涉及敏感信息或可利用安全影响的普通缺陷，可以直接提交公开 Issue。模型输出质量、第三方 AI 服务的计费或数据政策，以及上游依赖本身的漏洞，应优先向对应服务或上游项目报告；如果 TransVortex 的集成方式扩大了风险，再同时向本项目私密报告。

项目当前不承诺固定响应时限。维护者会先确认报告是否属于本项目、评估受影响版本，并在修复和发布安排明确后与报告者协调披露；请在公开细节前留出合理的修复时间。
