# TransVortex 项目设计说明书

本文描述当前产品与系统边界。短期待办以 [`docs/CURRENT_BACKLOG.md`](docs/CURRENT_BACKLOG.md) 为准，具体配置、运行和前端规格由对应专题文档负责。

## 1. 产品定位

TransVortex 是本地优先的字幕制作工具：输入视频、音频或 SRT，完成语音识别、翻译、字幕质量处理和导出。

产品同时提供两类入口：

- Windows Flutter 桌面应用，面向日常使用和任务处理。
- CLI / Agent 接口，面向脚本、自动化和高级用户。

两类入口共享同一套 Python core、任务工件、配置、凭据解析和 Worker，不各自实现业务流程。

## 2. 当前能力

- 视频自动检查内嵌文本字幕；没有可用字幕轨时再运行 ASR。
- 音频通过 ASR 生成带时间轴的 source segments。
- SRT 可以跳过 ASR，直接翻译和导出。
- ASR 支持受管本机 Whisper、用户提供的 FunASR 服务和 OpenAI Transcriptions 云端路径。
- 翻译支持可配置 provider、模型、routing fallback、容量感知分片、上下文、校验和修复。
- 术语记忆支持预设术语表、全片初始化、翻译注入和运行中动态维护。
- 输出支持 SRT、ASS、WebVTT 和 LRC；当前前端只把 SRT 作为字幕输入。
- 任务支持事件、checkpoint、取消、继续、结果编辑和重新导出。

## 3. 核心流程

```text
Input
  -> media inspection / source normalization
  -> ASR or embedded subtitle extraction
  -> source/segments.normalized.jsonl
  -> translation memory bootstrap and injection
  -> capacity-aware translation chunks
  -> deterministic validation and bounded repair
  -> alignment and subtitle quality processing
  -> final structured segments
  -> SRT / ASS / VTT / LRC renderers
```

结构化 `Segment` 是业务真实来源。ASR 原始响应、SRT、ASS 等格式不能反向成为翻译主状态。

## 4. 系统结构

```text
Flutter Desktop ─┐
CLI / Agent ─────┼─> Python core / task protocol
                 │       ├─ Local Service control plane
                 │       ├─ isolated Worker processes
                 │       ├─ artifacts and events
                 │       └─ provider / ASR adapters
                 └─> result workspace and exporters
```

- Local Service 负责配置、凭据引用、任务队列、状态、结果和 Worker 调度。
- Worker 负责 ASR、翻译、质量处理和导出等长任务。
- Flutter 窗口只负责展示、输入和系统集成，不持有业务权威状态。
- 任务、事件、配置和凭据文件是可恢复的权威状态；进程内状态只用于连接、调度和展示缓存。

后端分层见 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)，桌面进程模型见 [`docs/DESKTOP_APP_LOCAL_SERVICE_ARCHITECTURE.md`](docs/DESKTOP_APP_LOCAL_SERVICE_ARCHITECTURE.md)。

## 5. 数据与凭据

Windows 全新安装把可增长数据和程序放在用户选择的同一个产品根内，但使用独立删除边界：

```text
<TransVortexRoot>\
  App\
  Data\Tasks\
  Data\Cache\
  Resources\Components\
  Resources\Models\
  Resources\Downloads\ASR\

%LOCALAPPDATA%\TransVortex\Config\
```

- 升级只替换 `App`；`Data` 和 `Resources` 不进入程序删除范围，卸载默认保留用户数据。
- 可重建媒体缓存成功后清理，失败或取消时保留以支持恢复。
- 默认凭据文件是 `~/.transvortex/auth.json`。
- Provider YAML 只保存协议、模型、endpoint 和 `credential_id` 等非敏感引用。
- 仓库 `artifacts/` 只属于显式仓库 CLI 和开发实验，不自动进入正式桌面任务历史。

详细规则见 [`docs/CONFIG_GUIDE.md`](docs/CONFIG_GUIDE.md)。

## 6. Windows 交付

基础安装包包含：

- Flutter Windows 应用。
- 固定 Embedded Python 主 runtime 和已安装的 TransVortex wheel。
- 固定 FFmpeg LGPL shared runtime。
- NSIS 用户级安装器、开始菜单快捷方式和卸载入口。

基础包不包含本机 Whisper runtime、模型或 CUDA。这些组件由用户在语音识别设置中按需安装。

当前 `0.1.0` Alpha 内部安装包已经通过本机安装、升级、运行中保护、固定 runtime、快捷方式、卸载和用户数据保留验收。公开发布仍需要代码签名、FFmpeg 对应源码托管、干净 Windows 真实任务和已安装路径通知验证。

详细边界见 [`docs/APP_RUNTIME.md`](docs/APP_RUNTIME.md) 和 [`docs/LOCAL_ASR_COMPONENTS.md`](docs/LOCAL_ASR_COMPONENTS.md)。

## 7. 产品原则

- 本地优先：媒体默认在本机处理；只有用户选择的远端 ASR 会上传音频，翻译 provider 只接收必要文本和上下文。
- 可恢复：长任务增量落盘，失败后从已完成阶段继续。
- 可审计：原始识别、模型请求分类、翻译修复、质量报告和输出记录可追溯。
- 明确失败：缺配置、目录不可写、组件缺失和外部服务失败必须给出具体恢复动作。
- 单一业务实现：桌面、CLI 和 Agent 不分叉 pipeline。
- 格式与业务分离：字幕 renderer 不承担 ASR、翻译或术语逻辑。

## 8. 暂缓范围

- 托盘、窗口关闭后的后台驻留和最终 Supervisor 归属。
- 完整术语管理和跨任务复用界面。
- 完整结果编辑器、跨任务批量筛查和高级导出复核。
- 多 Worker 并发、实时字幕和完整多厂商 ASR gateway。
- macOS / Linux 正式交付。

这些事项是否进入近期开发，以 [`docs/CURRENT_BACKLOG.md`](docs/CURRENT_BACKLOG.md) 为准。
